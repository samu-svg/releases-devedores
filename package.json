{
  "name": "cobranca-sistema",
  "private": true,
  "version": "1.1.0",
  "description": "Sistema de cobrança — web (Vercel) e desktop (Electron)",
  "scripts": {
    "build": "npm run build --prefix backend && npm run build:web --prefix frontend",
    "dev:web": "npm run dev --prefix backend & npm run dev:web --prefix frontend"
  }
}
